---
title: "What to Ask Before Joining a PhD Group"
excerpt: "The questions I wish more students asked before committing to an advisor — and why 'strong publication record' isn't the only thing that matters."
header:
  overlay_color: "#8A2D3B"
  overlay_filter: "0.20"
  overlay_image: /assets/images/selecting_group_hero.png
  teaser: /assets/images/selecting_group_teaser.png
categories:
  - choosing-an-advisor
tags:
  - advisor-selection
  - grad-school-decisions
featured: true   # shows this post in the "From the Blog" section on the homepage
---

Replace this with your existing draft post. This file shows the front matter
structure the theme expects: a `title`, an `excerpt` (used in the blog list
and social previews), an optional `header` image/teaser, and `categories` /
`tags` for organization.

A few conventions worth keeping consistent across your posts:

- Keep `categories` broad (a handful you reuse across posts) and `tags` more
  specific — this keeps your archive pages useful rather than cluttered.
- Filenames must follow `YYYY-MM-DD-title-slug.md` — the date becomes part
  of the post's metadata (and, depending on your permalink settings, its URL).
- Use the `excerpt` field deliberately — it's what shows up when this post
  is shared on social media or listed in the blog index, so it's worth
  writing as a real hook rather than just the first sentence of the post.
